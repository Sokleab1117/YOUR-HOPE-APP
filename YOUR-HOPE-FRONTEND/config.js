const CONFIG = {
  // ─── Local development ──────────────────────────────────────────
  // API_BASE: 'http://localhost:5001/api',

  // ─── Production (Railway) ───────────────────────────────────────
  // After deploying to Railway, replace the URL below with your
  // actual Railway backend URL, then save and push to GitHub.
  API_BASE: 'https://YOUR-APP-NAME.up.railway.app/api',

  // ─── Gemini key is now handled server-side (removed from here) ──
  // Never put API keys in frontend code — anyone can see them.
};